{
  "name": "Lint Frontend on Save",
  "version": "1.0.0",
  "description": "Run ESLint on the frontend whenever a JSX or JS file is saved.",
  "when": {
    "type": "fileEdited",
    "patterns": ["frontend/**/*.js", "frontend/**/*.jsx"]
  },
  "then": {
    "type": "runCommand",
    "command": "npm run lint --prefix frontend"
  }
}
