{
  "name": "Security Check on Config Files",
  "version": "1.0.0",
  "description": "When config or environment-related files are edited, check for hardcoded secrets.",
  "when": {
    "type": "fileEdited",
    "patterns": ["**/config.py", "**/.env*", "**/settings/**"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A configuration file was just edited. Please verify: (1) no API keys, passwords, or secrets are hardcoded, (2) all sensitive values use environment variables, (3) defaults are safe for development use."
  }
}
