{
  "name": "Verify Backend After Task",
  "version": "1.0.0",
  "description": "After a spec task completes, verify the backend imports cleanly to catch broken dependencies early.",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": ".venv/bin/python -c 'from backend.api.routes import router; print(\"Backend OK\")'"
  }
}
