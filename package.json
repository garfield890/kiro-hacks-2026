{
  "name": "gym-form-checker",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": ".venv/bin/uvicorn backend.main:app --reload --reload-dir backend --port 8000",
    "dev": "npm run dev:frontend"
  }
}
